# 项目级协作约定

## 发布流程

- 当用户明确表示“要发布 / 发布到 npm / 发版”时，必须先完成以下动作，再进入发布命令阶段：
- 1. 更新 `package.json` 中的版本号
- 2. 更新 `CHANGELOG.md`，写清本次版本的主要变更
- 3. 再执行发布前检查与 `npm publish`

## 说明

- 不要在用户准备发布时直接执行 `npm publish`，除非已经确认版本号和 `CHANGELOG.md` 都已补齐。
- 如果当前版本号已经发布过，必须先递增版本号，不能重复发布同一版本。
